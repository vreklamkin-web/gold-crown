# Зависимости npm — устанавливаются локально командой npm install, в репозиторий не кладём.
node_modules/

# Папка со статической сборкой Eleventy (результат npm run build).
_site/

# Системные файлы macOS.
.DS_Store

# Логи npm.
npm-debug.log*
