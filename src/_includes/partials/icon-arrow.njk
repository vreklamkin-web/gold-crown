{#
  Переиспользуемая иконка-стрелка вправо для кнопок (.btn) и текстовых
  ссылок со стрелкой. Один SVG на весь проект — чтобы не хардкодить
  одну и ту же разметку в каждом месте, где нужна стрелка.
  Наследует цвет текста (currentColor), размер задаётся CSS-классом
  .icon-arrow (см. header.css).
#}
<svg class="icon-arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false"><path d="M5 12h14"/><path d="m13 6 6 6-6 6"/></svg>
