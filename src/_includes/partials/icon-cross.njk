{#
  Переиспользуемая иконка-крестик. Один SVG на весь проект — используется
  в "Avval / Endi" (колонка "Avval"); понадобится ещё в FAQ/форме
  (например для ошибок валидации). Цвет — currentColor, размер задаётся
  классом .icon-cross.
#}
<svg class="icon-cross" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true" focusable="false"><path d="M6 6l12 12M18 6 6 18"/></svg>
